package eventstream.grpc

fun main() {
    println("Hello from GRPC")

}

/*

Start the Server: Run main in Server.kt to start the gRPC server.

Run the Client:
Then, run main in Client.kt to start the client.

The client will send a Foo message to the server and print the response.

 */